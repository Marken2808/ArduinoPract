<script src="https://polyfill.io/v3/polyfill.min.js?features=default"></script>
<script
    src="https://maps.googleapis.com/maps/api/js?key=AIzaSyC14Hfofh1kVCUkY5BjoGqKfGmgX8lcy50&callback=initMap&libraries=&v=weekly"
    async
></script>
<script>

    function initMap() {

        const latlng = {
            lat: -34.397, 
            lng: 150.644
        };


        const map = new google.maps.Map(document.getElementById("map"), {
            center: latlng,
            zoom: 12,
        });

        new google.maps.Marker({
            position: latlng,
            map,
            title: "New Map"
        })
    }

    function updatemap(lat,lng) {
        var mylatlng = new google.maps.LatLng(lat,lng);
        var mapOption = {
            zoom: 12,
            center: mylatlng
        };
        var map = new google.maps.Map(document.getElementById("map"), mapOption);
        var marker = new google.maps.Marker({
            position: mylatlng,
            title: "Asset Number"
        });

        marker.setMap(map)
    }

    function updatelocation(id){
        updatemap(data[id].latitude, data[id].longitude);
    }

</script>